[alert_manager]

param.index = <string>
* Name of an index where Alert Manager events are written to.
* Defaults to 'main'

param.auto_assign_owner = <string>
* Assignee of new incident
* Defaults to 'unassigned'

param.impact = <string>
* Default impact for incidents
* Defaults to 'low'

param.urgency = <string>
* Default urgency for incidents
* Defaults to 'low'

param.title = <string>

param.auto_assign_owner = <string>
param.append_incident = [0|1]
param.auto_previous_resolve = [0|1]
param.auto_subsequent_resolve = [0|1]
param.auto_suppress_resolve = [0|1]
param.auto_ttl_resove = [0|1]

param.display_fields = <string>

param.urgency = [low|medium|high]
param.impact = [low|medium|high]

param.category = <string>
param.subcategory = <string>
param.tags = <string>

param.notification_scheme = <string>
